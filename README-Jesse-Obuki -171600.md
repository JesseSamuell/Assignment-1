# Assignment-1
Assignment one for DSA due on Friday 8th May

PART 4
1. What is the role of a class in a linked list?
- A class is basically a template on how to add, remove and basically the logic of how to manipulate the node, with information on the head, tail 
2. What is the difference between a node and a linked list?
  Node is made up of data and a pointer while a linked list is a collection of nodes.

3. Why do we use None in next?
   -It gives the code a safe point to stop in the case that it is iterating through items that may no longer exist, it also prevents iteration errors from happening.

4. How is a linked list different from a Python list?
   - a python list is contigous in nature so it stores all its data back to back and also their access time is instant as you just go to the Index
   - Linked lists are non-contigous and have a slower acces time at O(n)

5. Why is OOP useful for data structures?
   - OOP helps make code much more reliable and reusable as well as modular. This makes it easier to also apply stuff like encapsulation and abstraction and add things like pointers to data, making it possible to use data structures effectively
